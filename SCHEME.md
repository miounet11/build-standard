# 高质量软件方案

这是本工作室做软件的**唯一总纲**。创建细节在本仓库实践里；验收细节在 [ship-standard](https://github.com/miounet11/ship-standard)。两仓冲突时，以本页铁律为准。

---

## 一句话

**高质量不是写得更多，而是：人定做成、仓库只认一份权威、每笔改动先红后绿、主柱取最小值、用户装到才算修了、过期文档必须降级。**

---

## 系统全景

```
                    人（Owner）
              定北极星 · 定本轮做成 · 决定能不能发给下一台机器
                              │
                              ▼
┌──────────────────── 仓库记忆 ────────────────────┐
│  product/README   权威表：每类问题只认一份文件     │
│  roadmap          下一步只在这里                   │
│  spec/*           现在时不变量，和代码同一笔改     │
│  risks            发现就加，关闭不删               │
│  gates            哪盏灯 warn，哪盏灯 block        │
│  STATUS.md        脚本生成；人手改百分比 = 违规    │
│  docs/archive/    病例与旧「最新」只许住这里       │
└──────────────────────────────────────────────────┘
                              │
              Agent 按七步执行（缺一步就停）
         诊断 → 规格 → 计划 → 红灯 → 最小绿 → 门禁 → 反思
                              │
                              ▼
┌──────────────────── 验收（ship-standard）─────────┐
│  主柱 P = min(连/控/看/退 或产品自己的柱)         │
│  主路径 7 步勾完；两端同号；正式包                 │
│  用户通道 version ≥ 仓库减 1                      │
│  S1 = 0 才能标正式发布，否则只准 beta              │
└──────────────────────────────────────────────────┘
```

创建缺记忆，验收就会从聊天重讲。验收缺门禁，创建就会自我宣布完成。文档有多份「最新」，下一会话会读错并写错。

---

## 十条铁律

1. **完成由门禁说了算。** 任务清单、聊天「做完了」、本机绿，都不算。
2. **人拥有上线权。** Agent 不得宣布已上线、不得关生产守卫、不得把密钥写进仓库。
3. **每类问题只认一份现行文件。** 其余标历史或子集。文件名禁止出现「最新」。
4. **STATUS 只生成、不手改。** 打不开 STATUS，就不能回答「现在坏在哪」。
5. **不变量用现在时，写得出测试。** 「会更稳」不进规格。
6. **先红后绿，一层一个提交。** 禁止同一笔改四层；禁止功能提交里顺手重构。
7. **产品力取主柱最小值。** 禁止用旁边的功能补主路径失败。
8. **用户没装到 = 没修。** 分发 / OTA 落后超过一个次版本是质量事故。
9. **病例不是架构。** 新症状对不变量编号；并进规格后，旧 BUGFIX 进 archive。
10. **本季度不朝北极星移动的事，不做。** 「竞品有」不是立项理由。

---

## 仓库必须长成这样

新产品按这个建；旧产品按这个收，不必一天改完，但权威表必须先有。

```
product/
  README.md          ← 权威表（抄 templates/product-README.md）
  roadmap.md         ← 里程碑 + 本季度不做
  risks.md           ← S1 打开则正式发布失败
  quality-gates.md   ← 门禁矩阵
  spec/<纬度>.md     ← 一份纬度一份不变量
  baselines/         ← 包体、探针分数
docs/archive/        ← BUGFIX、旧联调、旧 RELEASE
STATUS.md            ← 生成
templates 不进产品仓；身份文件（clavue.md / AGENTS.md）只链到 product/，不另写进度
```

复制用的空模板在 [templates/](./templates/)。

---

## 一笔改动怎么走

非平凡改动（行为、结构、阈值、发布）必须走满：

| 步 | 做什么 | 停下来的信号 |
|----|--------|--------------|
| 1 诊断 | 3 条缺口，用户影响 | 说不清层，先不要改 |
| 2 规格 | 改且只改一份现行规格 | 找不到权威文件，先补权威表 |
| 3 计划 | 本轮只做 P0 | P1 进路线图，不进本提交 |
| 4 红灯 | 第一笔提交是失败探针 | 没有红灯，不准写实现 |
| 5 绿灯 | 最小 diff | 邻层脏了记风险，不顺手改 |
| 6 验证 | CI / 约定跑法 | 只有本机绿，不算 |
| 7 反思 | 风险 + 规格同一笔 | 「以后再写」= 没走完 |

平凡改动（错字）可以缩短，但不得借此改行为。

---

## 一版怎样才算高质量

同时满足，缺一条就降为 beta 或内测：

- [ ] 权威表存在，STATUS 能生成且与仓库 version 一致
- [ ] 打开的 S1 = 0
- [ ] 本范围门禁绿（单测、类型、约定的 E2E / 探针）
- [ ] 主路径 7 步已勾，或明确停在失败步并降通道
- [ ] 相关端 version 相同；用户通道落后 ≤ 1 个次版本
- [ ] 主柱没有一柱失败（P = min）
- [ ] 生产守卫未关（无 mock、无 localhost API、无密钥进包）
- [ ] 本轮新病例已并进不变量或已进 archive，没有新的「最新.md」

上架、品牌、会员可以准备，但**内核没过，不能标可提审**。

---

## 长项目怎么避免被文档拖死

这是远控和 IDE 长周期里付过的学费，升成制度：

| 病 | 制度 |
|----|------|
| 4～6 份都自称 SoT | [authority](./practices/authority.md)：根上只留权威表 |
| 手写状态互相打架 | [status](./practices/status.md)：只认生成的 STATUS |
| BUGFIX / 联调 0.2.x 变成地图 | [archive](./practices/archive.md)：病例归档，现行规格吸收不变量 |
| `/init` 冲掉手写约定 | 生成文件只链 `product/` |
| 验收清单永远空着还在发版 | 空清单 = 只能 beta |
| 文档里写现场账号 | 只许 `.env.*.example`，仓库正文禁止 |

---

## 季度怎么让质量变强（不要倒着走）

1. **看见** — 灯、风险、STATUS。默认先不改业务。
2. **改得动** — 拆层，架构探针锁入口行数和分层。
3. **写成合同** — 核心能力有基线和阈值；先 warn，两轮绿再 block。
4. **能力骑在合同上** — 新功能先有不变量和红灯。
5. **才商业化** — 商店、收费、团队功能。

倒着走（先上架再补内核，或先堆十个功能再拆入口文件）是假进度。

---

## 明确不追求的「完美」

- 不追求一次提示生成整盘。
- 不追求文档字数。现行文件短、过期文件进 archive，比再写一本手册优先。
- 不追求功能矩阵打满。主柱和主路径优先。
- 不追求无盲测的「已超越竞品」。
- 不追求把所有历史病例留在现行目录里「以备查阅」——查阅去 archive。

---

## 从今天开始

1. 每个产品仓根上放一份 `product/README.md` 权威表（用 [templates/product-README.md](./templates/product-README.md)）。
2. 把标题含「最新」的文档改名或搬进 `docs/archive/`。
3. 接一条生成 STATUS 的命令，哪怕第一版只输出 version + 打开的 S1 数。
4. 下一笔非平凡改动走七步；发版对照上面的清单。

做完这四步，方案已经在跑。其余实践按需打开，不必一天读完两仓。
