# 高质量软件方案

这是唯一总纲。创建细节在本仓库 `practices/`；上线判定在 [ship-standard](https://github.com/miounet11/ship-standard) 的门禁 id。三者冲突时以本页为准。

---

## 一句话

**高质量 = 完成定义可检查 + 权威只有一份 + 每笔改动先红后绿 + 主柱取最小值 + 用户装到才算修了 + 过不了就豁免而不是绕过。**

---

## 1. 因果模型：软件为什么会不高质量

不是「人不努力」，是四种结构性泄漏。所有铁律都由此推出，不是口味。

| 泄漏 | 现场表现 | 后果 |
|------|----------|------|
| **完成定义不可检查** | 任务清单打勾、聊天说「做完了」、本机绿 | 假完成累积，发版才爆 |
| **权威不唯一** | 4–6 份都自称 SoT，三份手写状态 | 下一会话读到过期结论并写进代码 |
| **反馈晚于改动** | 先写实现再补测试；一提交改四层 | 说不清根因，下一轮重付一次钱 |
| **修复到不了用户** | 仓库版本涨，分发通道不动 | 版本号在涨，产品力不涨 |

对应堵法：**门禁**（堵 1）、**权威表**（堵 2）、**先红后绿 + 最小变绿**（堵 3）、**真路径 + 通道对齐**（堵 4）。

判断一条新规则要不要加，只问：它堵的是哪个泄漏。堵不到就不加。

---

## 2. 系统全景

```
                    人（Owner）
        定北极星 · 定本轮做成 · 决定能不能发给下一台机器
                              │
                              ▼
┌──────────────────── 仓库记忆 ────────────────────┐
│  product/README   权威表：每类问题只认一份文件     │
│  roadmap          下一步只在这里                   │
│  spec/*           现在时不变量，和代码同一笔改     │
│  risks            发现就加，关闭不删               │
│  gates            引用 ship-standard 门禁 id       │
│  waivers          过不了的门禁 + 到期日 + owner     │
│  STATUS.md        脚本生成；手改百分比 = 违规      │
│  docs/archive/    病例与旧操作手册只许住这里       │
└──────────────────────────────────────────────────┘
                              │
              Agent 按七步执行（缺一步就停）
         诊断 → 规格 → 计划 → 红灯 → 最小绿 → 门禁 → 反思
                              │
                              ▼
        验收（ship-standard 门禁 id · 60 条）
   主柱取 min · 主路径关门 · 各端同号 · 通道落后 ≤1 · S1=0
```

---

## 3. 十条铁律

每条后面括号里是它堵的泄漏。

1. **完成由门禁说了算。**（1）任务清单、聊天、本机绿都不算。
2. **人拥有上线权。**（1）Agent 不得宣布已上线、不得关生产守卫、不得把密钥写进仓库。
3. **每类问题只认一份现行文件。**（2）其余标历史或子集；文件名禁止声称「当前唯一版本」。
4. **STATUS 只生成、不手改。**（2）打不开 STATUS，就不能回答「现在坏在哪」。
5. **不变量用现在时，写得出检查。**（1）「会更稳」不进规格。
6. **先红后绿，一层一个提交。**（3）禁止同一笔改四层；禁止功能提交里顺手重构。
7. **产品力取主柱最小值。**（1）禁止用旁边的功能补主路径失败。
8. **用户没装到 = 没修。**（4）分发落后超过一个次版本是质量事故。
9. **病例不是架构。**（2）新症状挂不变量编号；并进规格后旧病例进归档。
10. **过不了就写豁免，不要静默绕过。**（1）见 §6。静默绕过会让所有门禁失去信号。

---

## 4. 成熟度分级：不要一次全绿

新项目和五年老项目不该被同一套 block 判死。按级推进，**级内全绿再升级**。

| 级 | 目标 | 必须 block 的门禁 | 允许 |
|----|------|-------------------|------|
| **L0 看见** | 缺口从隐形变红灯 | `DOC-1` `DOC-2` `COMPOUND-1` `COMPOUND-2` `PROBE-2` | 业务逻辑先不改 |
| **L1 改得动** | 架构能吸收改动 | 加 `PROBE-3` `PROBE-4` `DIAG-1` `DIAG-2` `KERNEL-2` | 核心能力还没基线 |
| **L2 有合同** | 能力变成基线与阈值 | 加 `COMPOUND-4` `PROBE-5` `KERNEL-3` `LAUNCH-2` `LAUNCH-3` | 上架材料还没齐 |
| **L3 可上线** | 这版可以给用户 | 加全部 `release` stage 门禁 | — |

规则：

- 声明自己在哪一级，写进 `product/README.md`。
- **只准往上升，不准悄悄降级。** 降级要写豁免。
- 高于本级的门禁跑成 `warn`，两轮绿再升 block。
- 老项目从 L0 开始，不要假装在 L3。

---

## 5. 一笔改动怎么走

非平凡改动（行为、结构、阈值、发布）必须走满：

| 步 | 做什么 | 停下来的信号 |
|----|--------|--------------|
| 1 诊断 | 3 条缺口，写用户影响 | 说不清在哪一层，先不要改 |
| 2 规格 | 改且只改一份现行规格 | 找不到权威文件，先补权威表 |
| 3 计划 | 本轮只做 P0 | P1 进路线图，不进本提交 |
| 4 红灯 | 第一笔提交是失败探针 | 没有红灯，不准写实现 |
| 5 绿灯 | 最小 diff | 邻层脏了记风险，不顺手改 |
| 6 验证 | CI / 约定跑法，贴证据 | 只有本机绿，不算 |
| 7 反思 | 风险 + 规格同一笔 | 「以后再写」= 没走完 |

平凡改动（错字、注释）可缩短，但不得借此改行为。

---

## 6. 豁免机制：唯一合法的例外

标准没有合法出口时，人会**静默绕过**，于是所有绿灯都不再可信。所以给出口，但让它贵。

豁免单写在 `product/waivers.md`（模板：[templates/waivers.md](./templates/waivers.md)），必须有：

| 字段 | 要求 |
|------|------|
| 门禁 id | 引用 ship-standard 的 id，例如 `PRESHIP-7` |
| 原因 | 一句话事实，不写「时间来不及」以外的修辞 |
| 补偿动作 | 这段时间用什么代替（人工核对、缩小范围、降通道） |
| 到期日 | **必须有**，最长一个发布周期 |
| owner | 具体人 |

铁规：

- 过期的豁免 = 门禁失败。发布脚本必须检查日期。
- 同一 id 连续豁免两次 → 要么改产品，要么改门禁（走 CHANGELOG），不许第三次。
- `LAUNCH-5`（安装包无密钥）、生产守卫类门禁**不可豁免**。

---

## 7. 仓库必须长成这样

```
product/
  README.md          ← 权威表 + 当前成熟度级别（抄 templates/product-README.md）
  roadmap.md         ← 里程碑 + 本季度不做
  risks.md           ← S1 打开则正式发布失败
  quality-gates.md   ← 引用门禁 id，标 warn / block
  waivers.md         ← 豁免单，带到期日
  spec/<纬度>.md     ← 一份纬度一份不变量
  baselines/         ← 包体、探针分数
docs/archive/        ← 病例、旧联调、旧发布说明
STATUS.md            ← 生成
```

身份文件（`AGENTS.md` / `clavue.md`）只链 `product/`，不另写进度百分比（`DOC-6`）。

落地脚本：把 [templates/quality-gate.mjs](./templates/quality-gate.mjs) 复制进产品仓，`node scripts/quality-gate.mjs` 即可检查权威表、STATUS、S1、过期豁免。

---

## 8. 一版怎样才算高质量

同时满足，缺一条就降 beta 或写豁免：

- [ ] 权威表存在，声明了成熟度级别（`DOC-1`）
- [ ] STATUS 是生成物且与仓库 version 一致（`DOC-2`）
- [ ] 打开的 S1 = 0（`COMPOUND-5`）
- [ ] 本级门禁全绿，无过期豁免
- [ ] 主路径已勾完或明确停在失败步（`PATH-1`）
- [ ] 各端同 version，用户通道落后 ≤ 一个次版本（`PATH-3` `KERNEL-5`）
- [ ] 主柱没有一柱失败（`KERNEL-1`）
- [ ] 生产守卫未关（`PRESHIP-4`）
- [ ] 本轮新病例已归档，没有新的「当前唯一版本」文档（`DOC-4` `DOC-5`）

上架、品牌、会员可以并行准备，但**内核没过不得标可提审**（`PRESHIP-1`）。

---

## 9. 季度顺序：不要倒着走

1. **看见** — 灯、风险、STATUS。默认先不改业务。
2. **改得动** — 拆层，架构探针锁入口行数与分层。
3. **写成合同** — 核心能力有基线与阈值；先 warn，两轮绿再 block。
4. **能力骑在合同上** — 新功能先有不变量和红灯。
5. **才商业化** — 商店、收费、团队功能。

倒着走（先上架再补内核，或先堆十个功能再拆入口文件）是假进度。

---

## 10. 标准自己怎么进化

标准也会过期。进化只走一条路：

**新失败 → 判断它堵哪个泄漏 → 加一条门禁 id 或一篇实践 → 写 CHANGELOG。**

禁止：

- 再开第三本手册
- 原地改已公布 id 的判定（收紧就加新 id，旧的标 deprecated）
- 把某次实现的细节写进铁律

---

## 11. 明确不适用

不要把这套压在不该压的东西上，否则会被整体丢弃：

| 场景 | 怎么做 |
|------|--------|
| 一次性脚本、探索性 spike | 只守铁律 2（不泄密钥）和 10（别假装已上线）；不必建 `product/` |
| 原型给自己看 | 免七步，但产出必须标 `prototype`，不得进用户通道 |
| 纯文案 / 错字 | 缩短流程，不得借机改行为 |
| 外部依赖的既有流程 | 保留其流程，只补权威表和 STATUS |

也明确不追求：文档字数、功能矩阵打满、一次提示生成整盘、无盲测的「已超越竞品」、把历史病例留在现行目录「以备查阅」。

---

## 12. 从今天开始（四步）

1. 抄 [templates/product-README.md](./templates/product-README.md) 到产品仓 `product/README.md`，写上当前成熟度级别。
2. 声称「当前唯一版本」的文档改名或搬进 `docs/archive/`。
3. 复制 [templates/quality-gate.mjs](./templates/quality-gate.mjs)，接一条 `npm run gate`。
4. 下一笔非平凡改动走七步；发版对照 §8。

四步做完方案就在跑。其余实践按需打开，不必一天读完两仓。落地顺序细节见 [ADOPTION.md](./ADOPTION.md)。
