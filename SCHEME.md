# 高质量软件方案

## 边界（先看这个，避免第二套权威）

| 这个问题 | 权威 |
|----------|------|
| 为什么会不高质量、铁律、成熟度**规则**、豁免**规则** | 本页 |
| 七步怎么执行、分工、仓库记忆、最小变绿 | 本仓 [`practices/`](./practices/) |
| 门禁 **id / stage / severity / 是否可豁免 / 各级要求哪些** | [ship-standard `gates.json`](https://github.com/miounet11/ship-standard/blob/main/gates.json) |
| 洞察有没有落成可引用的定律 | [creativity-is-engineering](https://github.com/miounet11/creativity-is-engineering) |
| 这个模型在六面上能不能修交接本 | [ability-harness](https://github.com/miounet11/ability-harness) |
| 这轮迭代 / 升级 / 修 bug 有没有漏审查 | [review-harness](https://github.com/miounet11/review-harness) |
| 老仓库怎么接进来 | [ADOPTION.md](./ADOPTION.md) |

本页**不判定**任何一条门禁的含义，也不列举哪条 id 在哪一级阻断 —— 那会造出第二份映射。
本页只定义推导规则，具体清单由 `gates.json` 生成。

---

## 一句话

**高质量 = 完成定义可检查 + 权威只有一份 + 反馈先于改动 + 用户装得到 + 不可逆的事有闸 + 过期会被发现。**

---

## 1. 因果模型：软件为什么会不高质量

不是「人不努力」，是六种结构性泄漏。所有铁律都由此推出。

| # | 泄漏 | 现场表现 | 后果 |
|---|------|----------|------|
| **1** | **完成定义不可检查** | 任务清单打勾、聊天说「做完了」、本机绿、探针一闪一闪 | 假完成累积，发版才爆 |
| **2** | **权威不唯一** | 4–6 份都自称 SoT，三份手写状态 | 下一会话读到过期结论并写进代码 |
| **3** | **反馈晚于改动** | 先写实现再补测试；一提交改四层 | 说不清根因，下一轮重付一次钱 |
| **4** | **修复到不了用户** | 仓库版本涨，分发通道不动 | 版本号在涨，产品力不涨 |
| **5** | **不可逆伤害没有闸** | 密钥进包、关掉生产守卫、破坏性迁移、没有回滚路径 | 一次失误无法撤销 |
| **6** | **世界在变而仓库冻着** | 依赖 CVE、证书/描述文件过期、上游接口改字段、系统大版本 | 代码一行没动，产品坏了 |

对应堵法：**门禁**（1）、**权威表**（2）、**先红后绿**（3）、**真路径与通道对齐**（4）、**不可豁免的闸**（5）、**按期重验**（6）。

判断一条新规则要不要加，只问：**它堵的是哪个泄漏。** 堵不到，就不进铁律 —— 但这不等于它不重要，见 [§11](#11-明确不适用与明确未覆盖)。

> 这六条是从三个真实产品的复盘里归纳的，不是一套普适本体论。见 [examples/our-studio.md](./examples/our-studio.md)。泄漏 1 和 3 在时间尺度上相邻（可检查的完成定义 vs 检查发生的时机），保留为两条是因为它们的堵法不同：一个是写门禁，一个是改提交顺序。

---

## 2. 系统全景

```
                    人（Owner）
        定北极星 · 定本轮做成 · 决定能不能发给下一台机器
                              │
                              ▼
┌──────────────────── 仓库记忆 ────────────────────┐
│  product/README   权威表 + 成熟度级别 + 升级到期日 │
│  roadmap          下一步只在这里                   │
│  spec/*           现在时不变量，和代码同一笔改     │
│  risks            发现就加，关闭不删               │
│  quality-gates    引用 gates.json 的 id            │
│  waivers          追加写；到期日 + owner + 次数     │
│  STATUS.md        脚本生成；手改百分比 = 违规      │
│  docs/archive/    病例与旧操作手册只许住这里       │
└──────────────────────────────────────────────────┘
                              │
              Agent 按七步执行（缺一步就停）
         诊断 → 规格 → 计划 → 红灯 → 最小绿 → 门禁 → 反思
                              │
                              ▼
        验收：gates.json 按 stage 推导出本级门禁
   主柱取 min · 主路径关门 · 通道对齐 · S1=0 · 无过期豁免
```

---

## 3. 十二条铁律

括号里是它堵的泄漏。没有括号的规则不该在这张表上。

1. **完成由门禁说了算。**（1）任务清单、聊天、本机绿都不算。
2. **人拥有上线权。**（5）Agent 不得宣布已上线、不得关生产守卫、不得把密钥写进仓库、不得跑破坏性迁移。
3. **每类问题只认一份现行文件。**（2）其余标历史或子集；文件名与标题禁止声称「当前唯一版本」。
4. **STATUS 只生成、不手改。**（2）打不开 STATUS，就不能回答「现在坏在哪」。
5. **不变量用现在时，写得出检查。**（1）「会更稳」不进规格。
6. **先红后绿，一层一个提交。**（3）禁止同一笔改四层；禁止功能提交里顺手重构。
7. **探针不稳就修好或删掉，不准靠重试。**（1）会闪的绿灯是自动运行的静默绕过；同一探针闪两次即隔离，并记风险。
8. **有主柱的产品，产品力取最小值。**（1）禁止用旁边的功能补主路径失败。没有用户可见主柱的产品（库、编译器）跳过本条，在 §11 写明。
9. **用户没装到 = 没修。**（4）分发落后超过一个次版本是质量事故。
10. **病例不是架构。**（2）新症状挂不变量编号；并进规格后旧病例进归档。
11. **世界会变，仓库要按期重验。**（6）依赖、证书、上游接口、系统版本有到期登记；门禁除了 PR 触发，还要定时重跑。
12. **过不了就写豁免，不要静默绕过。**（1）见 §6。静默绕过会让所有门禁失去信号。

---

## 4. 成熟度：从 stage 推导，不手写清单

老项目不该被同一套 block 判死。规则只有一条：

> **在级别 `Ln`，一条门禁阻断，当且仅当它的 `stage` 属于该级别、`severity` 是 `block`、且它的纬度适用于本产品。高于本级的一律跑 `warn`。**

级别与 stage 的对应、以及每级具体是哪些 id，**由 [`gates.json`](https://github.com/miounet11/ship-standard/blob/main/gates.json) 的 `levels` 决定**，并生成在 [ship-standard STATUS](https://github.com/miounet11/ship-standard/blob/main/STATUS.md) 里。本页不复制那张表。

| 级 | 目标 | 对应 stage |
|----|------|------------|
| **L0 看见** | 缺口从隐形变红灯 | `see` |
| **L1 改得动** | 架构能吸收改动 | `+ change-safe` |
| **L2 有合同** | 能力变成基线与阈值 | `+ contract` |
| **L3 可上线** | 这版可以给用户 | `+ release` |

opt-in 纬度（如 `resilience`、`pre-ship`）只有产品在 `product/README.md` 里声明了才绑定。长跑接码系统需要 resilience，一个库不需要。

规则：

- 级别写进 `product/README.md`，**并写下一次升级的到期日**。到期未升 = 门禁失败，机制和豁免相同。
- **只准往上升。** 降级要写豁免；生成的 STATUS 记录当前级别，所以偷偷降级会出现在 git diff 里。
- `warn` 升 `block` 的条件必须可数：默认**连续两次 CI 全绿**。
- 老项目从 L0 开始，不要假装在 L3。

---

## 5. 一笔改动怎么走

七步：**诊断 → 规格 → 计划 → 红灯 → 最小绿 → 验证 → 反思**。

每一步做什么、什么信号该停下来，权威在 [practices/loop.md](./practices/loop.md)。本页不复制那张表。

只强调两条最常被跳过的：

- **第 4 步的第一笔提交必须是红的。** 没有红灯不准写实现。
- **「平凡改动」不是自己说了算。** 只有 diff 在文档与注释之外没有非空行改动，才算平凡。改了行为就走满七步。

---

## 6. 豁免机制：唯一合法的例外

标准没有合法出口时，人会**静默绕过**，于是所有绿灯都不再可信。所以给出口，但让它贵，并且**让它真的生效** —— 一个不被脚本读取的豁免单只是一张纸。

豁免写在 `product/waivers.md`（模板：[templates/waivers.md](./templates/waivers.md)）。

**生效方式**：一条在期的豁免把对应门禁从 `block` 降为 `warn`，**同时把发布通道强制为 beta**。它不会让门禁变绿。

| 字段 | 要求 | 机器检查 |
|------|------|----------|
| 门禁 id | 必须存在于 `gates.json` | 是 |
| 原因 | 一句话事实 | 否 |
| 补偿动作 | 这段时间用什么代替 | 否 |
| 到期日 | 必填，最长一个发布周期；无固定节奏则 ≤ 30 天 | 是 |
| owner | 具体人名。`—`、空、`TBD` 一律拒绝 | 是 |
| 次数 | 从追加写的历史里数出来，不是手填 | 是 |

铁规：

- 过期的豁免 = 门禁失败。
- **行只追加，不删除。** 关闭时把状态改成 `closed`，保留行 —— 删行会把「第几次」擦掉，那正是被滥用的地方。
- 同一 id 第 3 次豁免直接拒：要么改产品，要么改门禁（走 ship-standard 的 CHANGELOG）。
- `gates.json` 的 `nonWaivable` 里的门禁**不可豁免**。当前钉住：`LAUNCH-5`（安装包/仓库无密钥）、`LAUNCH-11`（第三条路径不靠公布主机地址）、`PRESHIP-4`（生产守卫）。以钉住的表为准，不要在本页另抄一份会过期的名单。这一组对应泄漏 5。

---

## 7. 仓库必须长成这样

```
product/
  README.md          ← 权威表 + 成熟度级别 + 升级到期日
  roadmap.md         ← 里程碑 + 本季度不做
  risks.md           ← S1 打开则正式发布失败
  quality-gates.md   ← 引用门禁 id，标 warn / block
  waivers.md         ← 追加写的豁免账本
  spec/<纬度>.md     ← 一份纬度一份不变量
  baselines/         ← 包体、探针分数
docs/archive/        ← 病例、旧联调、旧发布说明
STATUS.md            ← 生成
scripts/
  quality-gate.mjs   ← 抄 templates/，结构门禁
  render-status.mjs  ← 抄 templates/，生成 STATUS
```

身份文件（`AGENTS.md` / `CLAUDE.md`）只链 `product/`，不另写进度百分比。
空合同与两个脚本在 [templates/](./templates/)，抄进去就能跑。

---

## 8. 一版怎样才算高质量

**不要在这里对照清单。** 跑：

```bash
node scripts/render-status.mjs && node scripts/quality-gate.mjs
```

生成的 `STATUS.md` 会给出：当前级别、本级红灯、打开的 S1、通道落后、过期豁免、发布通道结论。
门禁清单本身由 `gates.json` 推导，不在本页重抄。

只有两条判定留在本页，因为它们是**规则**而不是清单：

1. 本级有任一 `block` 门禁红且**没有在期豁免** → 不能发。
2. 有任一在期豁免 → 可以发，但通道强制 beta，对外材料不得标正式发布。

---

## 9. 季度顺序：不要倒着走

1. **看见** — 灯、风险、STATUS。默认先不改业务。
2. **改得动** — 拆层，架构探针锁入口行数与分层。
3. **写成合同** — 核心能力有基线与阈值；先 warn，两轮绿再 block。
4. **能力骑在合同上** — 新功能先有不变量和红灯。
5. **才商业化** — 商店、收费、团队功能。

这五步和 §4 的四级是同一件事的两种说法，不是两套流程。倒着走（先上架再补内核，或先堆十个功能再拆入口文件）是假进度。

---

## 10. 标准自己怎么进化

**新失败 → 判断它堵哪个泄漏 → 加一条门禁 id 或一篇实践 → 写 CHANGELOG。**

禁止：

- 再开第三本手册
- 原地改已公布 id 的判定（收紧就加新 id，旧的标 deprecated）
- 把某次实现的细节写进铁律
- 加一条本页自己不检查的规则（自检见 `npm run verify`）

---

## 11. 明确不适用与明确未覆盖

### 不适用

| 场景 | 怎么做 |
|------|--------|
| 一次性脚本、探索性 spike | 只守铁律 2（不泄密钥、不关守卫）和 12（别假装已上线）；不必建 `product/` |
| 原型给自己看 | 免七步，但产出必须标 `prototype`，不得进用户通道 |
| 纯文案 / 错字 | 缩短流程，不得借此改行为 |
| 没有用户可见主柱的产品（库、CLI、编译器） | 跳过铁律 8，在 `product/README.md` 写明用什么代替（如 API 兼容性 + 基准） |
| 外部依赖的既有流程 | 保留其流程，只补权威表和 STATUS |
| 两个以上人类 owner | 本方案假设单一 owner + Agent。多 owner 必须在权威表里写明谁对冲突拍板，否则权威表本身会分裂 |

### 未覆盖（重要，但还没有门禁）

写在这里，是为了不让「堵不到泄漏就不加」变成假装这些问题不存在。不要为本仓新开纬度，也不要催 ship-standard 用已经拒绝的名字（`compat` / `secrets` / `observe` / `rollback` / `host`）再开一份。

| 缺口 | 现行归宿 | 现在怎么办 |
|------|----------|------------|
| 数据/配置迁移与向下兼容 | ship `PRESHIP-3`、`LAUNCH-10` | 破坏性迁移按铁律 2 处理：人拍板 + 可回滚 |
| 密钥与配置第三条路 | ship `LAUNCH-5` `LAUNCH-11` | 不进包、不靠公布主机地址 |
| 依赖与供应链（锁版本、CVE、SBOM） | 尚无 id | 按铁律 11 定期重跑，把证书与依赖到期写进风险册 |
| 可观测性与事故响应 | ship `RESIL-8`、`LAUNCH-10` | 日志能分清各类失败；回滚条件预先写死 |
| 性能与容量预算 | ship `COMPOUND-4` | 把预算写成基线，回退即红 |
| 无障碍与国际化 | 尚无 id | 有用户就写进 `spec/`，别等标准 |
| 成本（七步的时间与 token 代价） | 尚无 id | 承认它存在；这正是「平凡改动」被滥用的动机，所以 §5 把它变成可机器判断的条件 |

### 明确不追求

一次提示生成整盘、文档字数、功能矩阵打满、无盲测的「已超越竞品」、把历史病例留在现行目录「以备查阅」。

---

## 12. 从今天开始（四步）

1. 抄 [templates/product-README.md](./templates/product-README.md) 到 `product/README.md`，写上当前级别和升级到期日。
2. 声称「当前唯一版本」的文档改名或搬进 `docs/archive/`。
3. 抄 [templates/quality-gate.mjs](./templates/quality-gate.mjs) 和 [templates/render-status.mjs](./templates/render-status.mjs) 到 `scripts/`，跑一次。**第一次会红，这是对的** —— 红在哪就是你现在的真实状态。
4. 下一笔非平凡改动走七步。

四步做完方案就在跑。落地顺序与常见卡点见 [ADOPTION.md](./ADOPTION.md)。
